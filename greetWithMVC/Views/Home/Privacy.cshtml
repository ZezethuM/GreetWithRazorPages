@* @{
    ViewData["Title"] = "Privacy Policy";
}
<h1>@ViewData["Title"]</h1>

<p>Use this page to detail your site's privacy policy.</p>

<div class="widget">
        <table class="table table-stripped table-bordered">
            <thead>
                <tr>
                    <th>FriendsName</th>
                    <th>Count</th>
                    <th>Remove</th>
                </tr>
            </thead>
                @foreach(var key in @Model.GreetingF)
                {
                    <tbody>
                    <tr>
                        <td>@key.Key</td>
                        <td> @key.Value</td>
                        <td> 
                            <form method="post">
                                <button asp-page-handler="remove" type="submit" asp-route-name="Name" class="btn btn-danger">Remove</button>
                                <input type="hidden" name="Name" value="@key.Key"/>
                            </form>
                        </td>
                    </tr>
                    </tbody>
                }
        </table>
    <form method="post" class="clearBtn" asp-page-handler="delete">
        <button type="submit" class="btn btn-danger">Clear</button>
    </form>
    @ViewData["Name"]
</div> *@
